# Future Work

## 自由尺寸 grid（更策展）
用 `grid-auto-rows` 小基准单元 + 每张照片按宽高比计算 `grid-row: span N`，实现每张照片保持原始比例、大小错落的策展式排布。需要处理 resize 重算、第一行偏移逻辑复杂化、少量照片时的排布调优等问题。

## 设计侧边栏里的链接
为 Photography / Archive / About 设计实际的跳转目标和页面内容。包括：小红书、Instagram、邮箱的真实链接填入。
